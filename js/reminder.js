$( function() {
    $( "#datepicker" ).datepicker();
} );
// Used to send reminder data to database
function displayData() {
  document.getElementById('displayresult').innerHTML = "Reminder added!";
  //get elements by id and add to databse
}
